# Production Readiness Checklist

This project is currently a strong predictive-monitoring prototype. To call it production-ready for real downtime prevention, the following gates should be satisfied.

## 1. Model Reliability Gates

Minimum before enabling automated recovery:

- At least 1,000 labeled training samples.
- At least 300 `NORMAL` rows.
- At least 300 `WARNING` rows.
- At least 150 `CRITICAL` rows.
- At least 150 `RECOVERY` rows.
- `CRITICAL` recall >= 0.85.
- `RECOVERY` recall >= 0.75.
- Model report status is `production_candidate`.

Current training reports are generated by:

```powershell
python train_model.py --db build\Debug\monitor.db --model build\ai_model.joblib --meta build\ai_model_meta.json --report-json build\model_report.json --report-txt build\model_report.txt
```

## 2. Runtime Inference Gates

The app now supports a persistent local Python inference service. This avoids repeatedly launching Python for normal prediction cycles and is the default runtime mode.

Remaining upgrade:

- Convert the trained model to ONNX for fully native C++ inference.
- Track prediction latency and timeout counts.
- Keep fallback behavior when inference fails.

## 3. Auto-Heal Safety Gates

Before enabling real healing:

- Default to dry-run mode.
- Keep `AUTO_HEAL_ENABLED=0` until model readiness is `production_candidate`.
- Use Process Genome safety categories before any action.
- Use User Intent protection before any action.
- Use Stage 3 decision safety gates before any action.
- Maintain process and service allowlists.
- Maintain denylist for critical system processes.
- Require high model confidence.
- Require repeated critical state before action.
- Add cooldowns per process/service.
- Add audit logging for every recommendation and action.
- Require a Stage 4 dry-run plan before any action.
- Require pre-check, post-check, and rollback notes for every future action.
- Add manual override / disable switch.
- Verify the system improved after action.

## 4. Observability Gates

- Write structured application logs.
- Persist prediction source, risk, confidence, class, reason, and action recommendation.
- Persist process category, safety, waste score, and expected gain.
- Persist user intent state, foreground app, app kind, fullscreen state, and focus duration.
- Persist decision root cause, target, safety gate, cooldown, blocked reason, and dry-run status.
- Persist auto-heal plan status, readiness, target, expected impact, pre-check, post-check, and rollback note.
- Track SQLite write failures.
- Track Python service and future ONNX inference failures.
- Display model readiness, feature count, and training timestamp.

## 5. Release Gates

- Keep build artifacts out of git.
- Build from clean clone.
- Add CI for Python tests.
- Add CI or documented local check for C++ build.
- Package a release folder or installer.
- Document target Windows versions.

## 6. Portfolio Quality Gates

- Include real screenshots in `screenshots/`.
- Keep README truthful and concise.
- Add architecture diagram.
- Add short demo video or GIF.
- Include model report snapshot.
- Clearly label unfinished features as roadmap items.

## Current Honest Status

PredictiveAutoHeal is portfolio-ready as a predictive monitoring project with conservative safety gates and dry-run healing plans.

It is not yet production-ready for autonomous healing. The next production milestone is improving CRITICAL and RECOVERY reliability using scenario-labeled data.
